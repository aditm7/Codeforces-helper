#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
#include <set>
#include <map>
#include <string> //#include <bits/stdc++.h>

#define ll long long int
#define ld long double
#define endl "\n"
#define lb lower_bound
#define ub upper_bound
#define pii pair<int,int>
#define pll pair<ll,ll>
#define vi vector<int>
#define vll vector<ll>
#define vpii vector<pii >
#define vvll vector<vector<ll> >
#define all(v) (v).begin(),(v).end()
#define sz(x) (ll)x.size()
#define pb push_back
#define mp make_pair
#define test_case 	long int T;cin>>T;while(T--)
#define fori(i,a,b) 	for(long long int i=a;i<b;i++)
#define ford(i,a,b) 	for(long long int i=a;i>=b;i--)
#define fio	ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
const ll	mod=1e9+7;
#define ss		second
#define ff		first
#define ci(X)		long long X; cin >> X
#define cii(X, Y) 	long long X, Y; cin >> X >> Y
#define ciii(X, Y, Z) 	long long X, Y, Z; cin >> X >> Y >> Z
#define Max(a,b)		((a > b) ? a : b)
#define Min(a,b)		((a < b) ? a : b)
ll gcd(ll a,ll b){ if (b==0) return a;return gcd(b,a%b);}
ll fact(ll a){if (a==1) return 1;return a*fact(a-1);}
ll powermod(ll a, ll b) {ll ans = 1; while (b){ if (b & 1) ans = (ans * a) % mod; b = b / 2; a = (a * a) % mod;} return ans;}
using namespace std;

int main(){
	#ifndef ONLINE_JUDGE
	freopen("input1.txt","r",stdin);
	freopen("output.txt","w",stdout);
	#endif
	fio;
//main_function code goes here:
	test_case{}
	return 0;
}